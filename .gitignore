# 原始分页数据（量大，仅本地保留，不入库）
data/raw/
data/raw_comments/
data/raw_reposts/

# 原始未归一化全量（26MB，不入库）
data/posts_raw.json
data/posts_raw_pretty.json

# cookies 等敏感凭据（不入库）
data/xq_cookie.txt
data/xq_cookies.txt

# GitHub PAT（本地推送用，绝不入库）
.github_token

# 每小时整点归档（本地历史快照，不入库，避免仓库膨胀）
data/hourly/

# 编辑器 / 临时
.vscode/
.idea/
*.swp
*.log
.DS_Store
__pycache__/
*.pyc

# 临时探测脚本 / 中间文件（本地留痕，不入库）
scripts/_*.py
data/_*.json
